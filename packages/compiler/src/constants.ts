export const RUNTIME_VIRTUAL_ENTRYPOINT_PREFIX = 'runtime' as const
export const BROWSER_VIRTUAL_ENTRYPOINT_PREFIX = 'browser' as const
export const HTML_VIRTUAL_ENTRYPOINT_PREFIX = 'html' as const
export const API_VIRTUAL_ENTRYPOINT_PREFIX = 'api' as const
export const EXPRESS_VIRTUAL_ENTRYPOINT_PREFIX = 'express' as const

export const PREFIXES = [
  RUNTIME_VIRTUAL_ENTRYPOINT_PREFIX,
  BROWSER_VIRTUAL_ENTRYPOINT_PREFIX,
  HTML_VIRTUAL_ENTRYPOINT_PREFIX,
  API_VIRTUAL_ENTRYPOINT_PREFIX,
  EXPRESS_VIRTUAL_ENTRYPOINT_PREFIX,
]

export const TYPED_CONFIG_IMPORT = 'typed:config' as const

export const VIRTUAL_ID_PREFIX = '\0'

export const NAME = '@typed/compiler'
