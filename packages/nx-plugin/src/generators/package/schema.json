{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Package",
  "title": "Configuration for generating a new @typed package",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": [
    "name"
  ]
}
