/**
 * Selects need to be accessible
 *   - Touches should be mapped to the native select element for native behaviors
 *   - Only in the client should we replace the native select with our custom UI using portals
 * Multi-select
 */
