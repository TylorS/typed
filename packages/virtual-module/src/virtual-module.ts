// TODO

/**
 * TypeScript
 *
 * Modify ts.sys
 *   readFile
 *   fileExists
 *
 * Modify Language Service Host
 *   resolveModuleNameLiterals
 *   getScriptKind
 *   getScriptVersion
 *   getScriptSnapshot
 *   getFileNames
 */

/**
 * Virtual Modules
 *
 * 2 types of virtual modules
 *  - Directory - Requires watching for new files in this directory to recompute virtual module
 *  - File - Requires watching for changes to this file to recompute virtual module
 *
 * Resolve virtual module ids to virtual file paths
 * Generate and cache virtual file snapshots with version numbers
 *
 */
