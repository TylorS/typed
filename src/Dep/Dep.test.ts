describe(__filename, () => {
  describe('Dep', () => {
    it('constructs a memoized dependency graph', () => {})

    it('can be composed horizontally', () => {})

    it('can be composed vertically', () => {})
  })
})
