describe(__filename, () => {
  describe('FiberLocal', () => {
    it('creates a local value upon from the initial', () => {})

    it('retrieves a local value on additional get calls', () => {})

    it('can handle distributed updates atomically', () => {})
  })
})
