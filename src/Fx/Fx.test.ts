describe(__filename, () => {
  describe('Fx', () => {
    it('allows accessing a set of requirements', () => {})

    it('can represent errors', () => {})

    it(`always runs in a Fiber's Context`, () => {})

    it(`allows tracking Fiber-local state`, () => { })

    it('')
  })
})
