import { describe, given, it } from '@typed/test'

export const test = describe(`useState`, [
  given(`an InitalState`, [
    it(`keeps state between invocations`, ({}) => {}),

    it(`emits HookEvent when updated`, ({}) => {}),
  ]),
])
